# VuePress default temp directory
docs/.vuepress/.temp
# VuePress default cache directory
docs/.vuepress/.cache
# VuePress default build output directory
docs/.vuepress/dist
# node modules
node_modules
# Local data (large files with transcripts, used by scripts only)
data/
